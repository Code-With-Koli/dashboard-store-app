import 'antd/dist/reset.css';
import { Button } from 'antd';

const App = () => {
  return (
    <Button type='dashed' className='font-bold text-blue-500'>
      hi Mom
    </Button>
  );
};

export default App;
